LLM Analysis Quiz - Server

Run locally:
  pip install -r requirements.txt
  playwright install
  export SOLVER_LLM_API=...
  export EXPORTED_SECRET=...
  python app.py

Deploy:
  Push to GitHub
  Deploy on Render
